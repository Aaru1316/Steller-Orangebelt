[package]
name = "campaign"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21.6.0"
escrow = { path = "../escrow" }

[dev-dependencies]
soroban-sdk = { version = "21.6.0", features = ["testutils"] }
